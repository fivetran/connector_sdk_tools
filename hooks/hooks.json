{
  "hooks": {
    "BeforeAgent": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${extensionPath}/hooks/log-skill-use.sh\" \"${extensionPath}/gemini-extension.json\""
          }
        ]
      }
    ]
  }
}
