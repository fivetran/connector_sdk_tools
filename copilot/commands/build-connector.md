---
description: Build a new Fivetran connector from an API source
argument-hint: "Describe the connector (e.g., 'Stripe API connector for payments and customers')"
---

Invoke the `build-connector` skill from this plugin to guide the user through building a new Fivetran connector. The skill walks through requirements gathering, code generation, local testing, and deployment.

User request: $ARGUMENTS
