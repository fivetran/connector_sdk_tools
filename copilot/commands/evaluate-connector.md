---
description: Evaluate a Fivetran connector for correctness, SDK compliance, security, and reliability
argument-hint: "Connector directory name (e.g., 'github_connector')"
---

Invoke the `evaluate-connector` skill from this plugin to perform a static code review of the connector. The skill checks for SDK compliance violations, security issues, data reliability problems, and code quality, then produces a scored report.

User request: $ARGUMENTS
